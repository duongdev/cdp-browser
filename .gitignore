node_modules
dist
release

# Web build runtime state (default paths write to repo root; override via env)
web-settings.json
web-notifications.json

# Agent worktrees (transient, never commit)
.claude/worktrees/

# E2E run artifacts
test-results/
test/e2e/playwright-results.json
playwright-report/
